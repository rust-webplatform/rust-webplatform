[package]

name = "webplatform"
version = "0.1.3"
license = "MIT"
description = "APIs for the web platform for cross-compiled Rust to JS."
authors = ["Tim Cameron Ryan <tim@timryan.org>"]

[[bin]]

name = "webplatform"

[lib]
name = "webplatform"
path = "src/lib.rs"

[dependencies]
libc = "0.1.3"

[dependencies.concat_bytes]
path = "./concat_bytes"

